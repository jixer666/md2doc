<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.abc.biz.mapper.PointsFlowMapper">

    <resultMap type="PointsFlow" id="PointsFlowResult">
            <result property="flowId"    column="flow_id"    />
            <result property="userId"    column="user_id"    />
            <result property="ruleType"    column="rule_type"    />
            <result property="changePoints"    column="change_points"    />
            <result property="totalPoints"    column="total_points"    />
            <result property="createTime"    column="create_time"    />
            <result property="updateTime"    column="update_time"    />
            <result property="status"    column="status"    />
            <result property="ver"    column="ver"    />
    </resultMap>

    <sql id="selectPointsFlowVo">
        select flow_id, user_id, rule_type, change_points, total_points, create_time, update_time, status, ver from tb_points_flow
    </sql>

    <select id="selectPointsFlowList" resultMap="PointsFlowResult">
        <include refid="selectPointsFlowVo" />
        <where>
            <if test="userId != null">user_id = #{userId}</if>
        </where>
    </select>


</mapper>
