<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.abc.biz.mapper.PointsMapper">

    <resultMap type="Points" id="PointsResult">
            <result property="userId"    column="user_id"    />
            <result property="totalPoints"    column="total_points "    />
            <result property="availablePoints"    column="available_points  "    />
            <result property="frozenPoints"    column="frozen_points  "    />
            <result property="usedPoints"    column="used_points  "    />
            <result property="userType"    column="user_type"    />
            <result property="beginTime"    column="begin_time"    />
            <result property="endTime"    column="end_time"    />
    </resultMap>

    <sql id="selectPointsVo">
        select user_id, total_points, available_points, frozen_points, used_points, user_type, begin_time, end_time from tb_points
    </sql>

    <select id="selectPointsList" resultMap="PointsResult">
        <include refid="selectPointsVo" />
    </select>

    <update id="forzenPointsByUserId">
        update tb_points set available_points = available_points - #{points}, frozen_points = frozen_points + #{points}
        where user_id = #{userId} and available_points >= #{points}
    </update>

    <update id="unfreezePoints">
        update tb_points set available_points = available_points + #{points}, frozen_points = frozen_points - #{points}
        where user_id = #{userId}
    </update>

    <update id="reducePointsByUserId">
        update tb_points set used_points = used_points + #{points}, frozen_points = frozen_points - #{points}
        where user_id = #{userId}
    </update>

    <update id="increasePointsByUserId">
        update tb_points set available_points = available_points + #{points}, total_points = total_points - #{points}
        where user_id = #{userId}
    </update>

    <select id="selectByUserId" resultMap="PointsResult">
        <include refid="selectPointsVo" />
        where user_id = #{userId}
    </select>

</mapper>
